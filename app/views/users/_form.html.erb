<%= form_with(model: user, local: true, html: {multipart: true }) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username %><!--, id: :user_name-->
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %><!--, id: :user_name-->
  </div>
  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :text_field %>
  </div>
    <%= form.file_field :avatar, class: "green"%><!--, id: :user_name-->
  <div class="actions">
    <%= form.submit class: "btn", style: "margin-top: 10px" %>
  </div>
<% end %>
